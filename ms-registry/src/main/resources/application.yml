server:
  port: 8080

spring:
  application:
    name: ms-registry

# 配置Eureka注册中心
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    server-url:
      defaultZone: http://localhost:8080/eureka/


